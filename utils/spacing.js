import { StatusBar } from "react-native";

const variant = {
  lg: 20,
  md: 10,
  sm: 8,
  xs: 4,
};

export const spacing = {
  wrapper: "5%",
  statusbarHeight: StatusBar.currentHeight,
  "flex-row-spacing": 10,
  radius: {
    lg: 8,
    md: 4,
  },
  margin: variant,
  padding: variant,
};
